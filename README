# 步骤1：生成HLS代码（根据CDSE输出自动调整参数）
python3 generate_hls.py --config design_space/acc_config.json

# 步骤2：编译硬件
cd scripts && vivado -mode batch -source build.tcl


# 步骤3：编译主机程序
Makefile

# 步骤4：运行测试
./host design_space/tasks.json

rm -f host_executable *.o